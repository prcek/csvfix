{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch csvfix",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "args": ["-in", "input.csv", "-out", "output.csv"],
      "env": {},
      "cwd": "${workspaceFolder}"
    }
  ]
}
